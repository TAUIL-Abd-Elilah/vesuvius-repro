# Physical probability-bridge split: terminal capacity limitation

Status: **terminated without a scientific result**.

Attempt 12 ran the public amendment-06 lock unchanged. Its final exact
4,000,000,000-item watershed heap was exhausted naturally in the
`(PHerc1203, 960)` worker. The exact traceback is preserved in
`results/physical_bridge_split/preoutcome_failure_07.stderr.txt` and bound by
the adjacent machine-readable receipt. Stdout was empty, the worker and parent
exited, the temporary heap was cleaned, and no result file existed.

That worker key occurs in both frozen splits and no progress artifact was
persisted. No development comparison, selected candidate, partial row,
candidate mask, holdout result, or bridge outcome was persisted, exposed, or
inspected. The execution therefore supports neither a positive nor a negative
bridge-efficacy conclusion.

Amendment 06 was explicitly final. Its frozen exhaustion rule says to abort
without a result and terminate this protocol. There will be no retry, capacity
increase, candidate change, or selective rerun. This record documents an
operational capacity limitation only.
